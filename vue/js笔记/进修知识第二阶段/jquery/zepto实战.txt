1. attr与prop的区别
    /*
     * 1、prop多用在标签的固有属性，布尔值属性。比如：a标签的href，class，selected等。
     * 2、attr多用在自定义属性上。
     * 注意: 在jquery中如果用attr去获取布尔值属性且该布尔值属性在标签体内没有定义的时候，会返回undefined
zepto中attr可以获取布尔值属性
     */
remoteattr
remoteprop   1.2以上支持

2.配置属性$('<p>我是p</p>', { id: 'p1',,class:'p1','data-author':'bang' })

3.each
jquery可以遍历数组,对象   不可以遍历字符串 json字符串   zepto可以遍历字符串   json字符串
each(arr,function(item,index){ } )



4.offset()  偏移量  返回一个对象
jquery  中
   offset().top  offset().left    没有offset().width
   width()      //200
   css('width')  //200px
   innerHeight()  
   css('width')   css('padding')
   outerHeight()

zepto
    width()    //222 包含padding  和 border   隐藏display:none   拿到的值是0
    css('width')     //200px
    css('padding')  //10px
    css('border-width')  //1px

5.事件委托
jquery中
delegate('.a','click',function(){} )  
on.('click','.a',function(){} )


6.隐藏元素jq可以获取width()   zepto为0


addClass()  removeClass()
重绘  重排


jquery:
on()  绑定事件处理程序
off() 方法移除用目标oon绑定的事件处理程序。
bind()  为每个匹配元素的特定事件绑定事件处理函数，可同时绑定多个事件，也可以自定义事件。
one() 为每一个匹配元素的特定事件（像click）绑定一个一次性的事件处理函数。只执行一次。
trigger() 触发有bind定义的事件（通常是自定义事件）参数放在数组里['bang','value']
unbind()  bind的反向操作，删除匹配元素所绑定的bind事件。

zepto:
touch方法
* tap()点击事件 利用在document上绑定touch事件来模拟tap事件的，并且tap事件会冒泡到document上
* singleTap()  点击事件
* doubleTap()  双击事件
* longTap()    当一个元素被按住超过750ms触发。
* swipe, swipeLeft, swipeRight, swipeUp, swipeDown — 当元素被划过(同一个方向滑动距离大于30px)时触发。(可选择给定的方向)
在一个方向滑动大于30px即为滑动。否则算点击。
css样式:  touch-action: none;


6. form表单  serialize()  serializeArray()
    var list = $('form').serialize();
    console.log(list);
    //serializeArray()
    var listArr = $('form').serializeArray();
    console.log(listArr);
    //submit()
    $('form').submit(function(e){
        e.preventDefault();
        var data = $(this).serialize();
        var data2 = $(this).serializeArray();
        console.log(data);
        console.log(data2);
    })
  })


7.ajax
node bin/www
ctrl +c 停掉服务器
package.json文件中  "xxx": "node ./bin/www"     npm  run  xxx也可以启动服务器

disabled限制不了touch事件 
节流 设置一个变量  isMove = false


============实战
步骤:
1.添加页面元素
2.写css样式
3.写animation动画  
  -1.@-webkit-keyframes  里面的不用加webkit就好  不用正常的
  -2.@keyframes  里面的加上正常的和webkit
  -3.transform-origin: 50% 100%;
  -4.animation-timing-function: ease-out;
4.写js交互
1. var direaction = {up:1,right:2,bottom:3,left:4}   last ={ row : 0, col : 0} now = {row: 1, col:1}
2. 写移动函数 movePage  需要知道出去的page和进来的page
3. swipeUp添加滑动事件   注意要管理touch事件默认
*{ touch-action: none;}
4.setTimeout  img在页面显示后才显示  
5.节流   定义一个变量